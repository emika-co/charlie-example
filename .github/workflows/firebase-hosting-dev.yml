# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Dev Channel
'on':
  push:
    branches:
      - dev
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci --prefix ./src && npm run build --prefix ./src && npm run generate --prefix ./src
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_CHARLIE_296709 }}'
          channelId: dev
          projectId: charlie-296709
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
          ENV: production
